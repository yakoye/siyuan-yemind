/**
 * YeMind Zen source entry.
 *
 * v0.1.0 keeps the proven SiYuan shell from the previous prototype as the
 * distributable fallback while the editor core is migrated to simple-mind-map.
 * New development should be added under src/ rather than patched into index.js.
 */
import YeMindZenPlugin from "./plugin/YeMindZenPlugin";
export default YeMindZenPlugin;
