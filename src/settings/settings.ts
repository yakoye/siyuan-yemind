import type { Plugin } from "siyuan";

export function registerSettings(plugin: Plugin): void {
  // Settings sections: General and Shortcuts. No account, payment, trial,
  // activation, membership, or feature-gating concepts exist in YeMind Zen.
  void plugin;
}
