import type { Plugin } from "siyuan";

export function registerYeMindDock(plugin: Plugin): void {
  // Dock implementation is migrated from the working runtime in small steps.
  // The stable behavior target is: one list, active-item sync, create, rename,
  // copy link, delete confirmation, and immediate stale-item cleanup.
  void plugin;
}
