# YeMind Zen v0.7.1 迁移状态

- 当前稳定版本：v0.7.1。
- 导图和检查点数据结构未升级，不需要迁移用户数据。
- v0.7.1 仅修复全局搜索关闭与打开导航适配器，不改变搜索索引或导图数据。
- v0.6.x 的导图、样式、备注、批注、概要、折叠和检查点继续兼容。
- 新增的关于、构建信息和诊断 schema v3 只影响插件设置与导出文件，不写入导图正文。
- 旧诊断包仍可人工读取；v0.7.0 新包按独立文件拆分，便于定位全局搜索链路。
- 发布版本开始遵循 `Major.Minor.Patch`：兼容性破坏、完整功能/子系统、单一修复分别决定主/次/修订号。
