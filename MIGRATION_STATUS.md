# YeMind Zen v0.6.8 迁移状态

## 当前状态

- 导图数据格式无需迁移。
- 搜索索引仍直接读取当前 `MapRepository`，不会向思源文档数据库写入影子块。
- v0.6.7 位于搜索输入区的旧结果面板会在首次搜索时自动清理。
- 新版结果挂载到思源 `#searchList`，预览以覆盖层方式进入 `#searchPreview`，不会销毁宿主 Protyle。
- 已保存的节点、备注、批注、标签、链接、待办、整图样式和检查点数据均保持兼容。
- v0.5.10 至 v0.6.7 的全部交互契约继续纳入回归。
