# YeMind v1.1.1 验证记录

日期：2026-07-27

## 自动验证

- `npm run check`：TypeScript 类型检查通过。
- `npm test`：15 个测试文件通过，661 项通过，45 项按既有条件跳过。
- `npm run test:web`：5 个测试文件通过，15 项通过。
- `npm run test:offline`：17 组离线烟测全部通过。
- `npm run test:syntax`：401 个 TypeScript 文件语法检查通过。
- `npm run check:version`：8 个发布标记全部为 `1.1.1`。
- `npm run verify:assets`：固定本地资源完整。
- `npm run build`：思源插件生产构建成功。
- `npm run build:web`：独立网页版生产构建成功。

## Chromium 验证

- 画布菜单在 1248×720 视口底部打开时完整可见，结构子菜单自动换边并保持在视口内。
- 画布菜单包含结构、主题、线型；节点和大纲菜单的“添加”子菜单包含公式、图片和剪贴图。
- 根节点折叠/展开时渲染节点数为 `5 → 1 → 5`。
- 中间节点折叠/展开时渲染节点数为 `5 → 6 → 5`。
- 长中英文混排文本在节点边框内自动换行。
- 选中文字后工具栏显示自包含 `Σ` 公式图标。
- 网页版显示版本 `v1.1.1`。

## 运行目录

- 已使用白名单同步脚本原位同步 810 个运行文件到 `D:\myDatabase\SiYuan\data\plugins\siyuan-yemind`。
- 运行目录 `VERSION` 为 `1.1.1`。
- `VERSION`、`plugin.json`、`index.js`、`index.css` 与提交工作区 SHA-256 一致。
