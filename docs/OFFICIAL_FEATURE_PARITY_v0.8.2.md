# YeMind v0.8.2 Dock 图标主题适配边界

思源 Dock 激活状态通过宿主 CSS 改变前景色。只有使用 `currentColor` 的 SVG 路径才能自动适配；嵌入 PNG 的像素颜色不会随宿主改变。

v0.8.2 将交互入口的图标改为纯 SVG：

- Dock、顶栏、菜单：`currentColor` 矢量图标；
- 关于页、品牌展示：#176B50 透明 PNG；
- 不增加针对某个主题的硬编码白色或黑色规则。

这样可兼容浅色、深色、自定义主题及 Dock 激活状态。
