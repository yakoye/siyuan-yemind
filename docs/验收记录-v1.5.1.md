# YeMind v1.5.1 验收记录

## 范围

- 基础编辑事务：导图粘贴尺寸同步、大纲 UID 文本补丁、快速反向选区删除。
- 剪贴板：边界空行清理、普通段落与结构化多行粘贴分类。
- 富文本：旧数据块格式无损迁移、画布/大纲挖空互操作、工具栏不透明无阴影。
- 界面：网页版侧栏重阴影清理、卡片菜单创建/删除状态。
- 双端：同一源码构建网页端和思源插件，并同步运行目录。

## 自动化结果

| 项目 | 结果 |
| --- | --- |
| 干净依赖安装 | `npm ci` 通过；安装 285 个包 |
| 版本一致性 | `1.5.1`，8 个发布标记一致 |
| TypeScript / 语法 | 449 个 TypeScript 文件语法通过；`tsc --noEmit` 通过 |
| 单元与集成 | 15 个测试文件；890 项通过，45 项按既有条件跳过 |
| 网页适配 | 6 个测试文件；22 项通过 |
| 离线冒烟 / 资源 | 17 组离线入口通过；固定资源完整 |
| Chromium E2E | 桌面与 Pixel 5 共 43 项通过，7 项按视口限定跳过 |
| 双端构建 / 发布包 | `release:build`、`release:verify` 通过；2 个 ZIP 与 `SHA256SUMS` 校验通过 |
| 思源运行目录 | 809 个文件同步；`index.js`、`index.css`、`plugin.json`、`VERSION` 与源码构建 SHA-256 一致 |
| 思源真实宿主 | SiYuan 3.7.3 冷启动加载 v1.5.1；旧测试图、大纲同步、边界空行修复和不透明工具栏实机通过 |
| GitHub Pages | 待提交推送后验证 |

## 人工验收结论

- 真实 Chromium 已验证：粘贴后边框、反向选区删除、工具栏操作、搜索替换、主题单树切换、侧栏阴影和导出关闭。
- 真实思源已验证：冷启动使用新构建；旧测试图正常加载，大纲无边界空行，选中文字工具栏为不透明无阴影面板且点击空白可关闭。
- 块级富文本迁移和跨表面挖空由存储/控制器集成测试覆盖；旧格式不会在加载迁移时被降级。
- 待发布后验证：GitHub Pages 版本、首页加载、建图、编辑、导入导出和控制台。

## 发布物

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `siyuan-yemind-v1.5.1.zip` | 3,864,234 | `d6a831dfafaaf66afb21057f9ca97efe0b1b69cb74828691dfd1d3afa7a7f451` |
| `yemind-web-v1.5.1.zip` | 3,648,340 | `e64b25a23a8222b9c8095f4090b2adc9ee099c630cde5f102d5ce36df92c8cdc` |
