# YeMind v0.6.9 官方功能适配边界

- 复用思源原生全局搜索列表、预览布局、键盘操作提示和关闭行为。
- YeMind 结果使用原生列表样式，但索引来自插件本地导图仓库。
- 大纲预览为轻量插件渲染，不在搜索窗口初始化完整思维导图编辑器。
- 打开和定位复用 YeMind 现有标签页、待定位 UID 与 `simple-mind-map` 原生命令。
- 不修改思源数据库、全文索引文件或原生搜索结果数据。
