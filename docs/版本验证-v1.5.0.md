# YeMind v1.5.0 版本验证

验证日期：2026-07-28

## 结论

v1.5.0 已通过版本、类型、核心、网页、离线、资源、浏览器、双端构建和发布包门禁。公开 Pages 已部署并完成成品页面复核，GitHub Release 已从最终标签提交生成并完成下载校验。

## 自动化结果

| 门禁 | 结果 |
| --- | --- |
| `npm run check:version` | 8 个版本标记均为 `1.5.0` |
| `npm run check` | TypeScript 无错误 |
| `npm test` | 15 个域、246 个场景模块；802 通过、45 条件跳过 |
| `npm run test:web` | 6 个文件、20 通过 |
| `node --check index.js` | 通过 |
| `npm run test:offline` | 17 组构建后冒烟全部通过 |
| `npm run verify:assets` | 126 标号、764 剪贴图、28 结构完整 |
| `npm run test:e2e` | Chromium 桌面/移动 26 通过、2 视口条件跳过 |
| `npm run release:verify` | 2 个 ZIP、manifest、SHA-256 和包结构通过 |
| GitHub Pages | 公开 `VERSION` 为 `1.5.0`；导图/大纲/卡片/复习、上下工具栏和暗色界面实测通过 |
| GitHub Release | 本轮 Version47 对齐产物覆盖到既有 `v1.5.0` Release；4 个发布文件按新清单复核 |

本轮 Version47 二次对齐在上述基础上重新执行了全部门禁：`npm run check`、`npm test`、`npm run test:web`、`npm run test:offline`、`npm run verify:assets`、`npm run test:e2e`、`node --check index.js`、`npm run release:build` 和 `npm run release:verify` 均通过。浏览器门禁为 26 通过、2 个按项目视口条件跳过，控制台为 0 error、0 warning。

## 实机证据

- SiYuan 3.7.3 从 `D:\myDatabase\SiYuan\data\plugins\siyuan-yemind` 加载 v1.5.0。
- 运行目录共有 809 个发布文件；`index.js`、`index.css`、`plugin.json`、`VERSION` 与本地候选哈希一致。
- 202 节点旧导图可打开、切换导图/大纲/卡片/复习、保存并恢复原主题。
- “经典”九张主题卡均为六色；实机应用“永恒”后六种连线色全部出现，随后恢复用户原“彩虹”主题。
- 深色外观下“彩虹”主题 ID 保持不变，画布为 `#111318`，节点、连线、缩略图和各面板可读。
- YeMind 控制台没有新增 error/warning；观察到的错误来自独立插件 `siyuan-plugin-open-md`，不属于本发布包。

## 发布物

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `siyuan-yemind-v1.5.0.zip` | 3,842,889 | `d3fbdc825828ac743f23416b2d16731a383d23552e1abb2a58d37646561800a9` |
| `yemind-web-v1.5.0.zip` | 3,631,380 | `72920d052cbbffadfaad1423a08ba34b65389178d2eb60773add513c283b719f` |

上表为本轮 Version47 二次对齐的可复现候选产物；正式发布后继续以公开 `release-manifest.json` 与 `SHA256SUMS` 逐项核对文件名、字节数和 SHA-256。

## 上游依赖审计

`npm audit`：0 critical、10 high、1 moderate、1 low。High 来自 vendored `simple-mind-map` 中未进入浏览器运行路径的 ESLint/Tern/Glob 工具依赖；UUID 当前只使用不受该告警调用条件影响的 v4；Quill 为低风险 HTML 导出项。本版不采用会跨主版本替换依赖树的 `npm audit fix --force`，升级工作已进入开发计划。

## 2026-07-29 未发布候选验证

当前工作区在既有 v1.5.0 发布基础上继续修复，尚未 commit、push 或更新 Pages/Release。

| 门禁 | 本地候选结果 |
| --- | --- |
| `npm run check` | 通过 |
| `npm test` | 845 通过、45 条件跳过 |
| `npm run test:web` | 20 通过 |
| `npm run test:offline` | 17 组通过 |
| `npm run verify:assets` | 通过 |
| `npm run build:all` | 插件、网页版通过 |
| `npm run test:e2e` | 28 通过、2 条件跳过 |
| 思源实机 | CRUD、拖动、折叠、结构、主题、搜索、图片、大纲分栏通过 |
| 真实传输 | 13 种导出完成；当前/旧版/大纲格式真实导入通过 |

详细步骤与预期限制见 [验证记录-2026-07-29.md](验证记录-2026-07-29.md)。

### 2026-07-29 样式面板增量验证

- 思源 3.7.3 实机：主题卡单击后画布即时换色，关闭未应用主题后恢复原主题。
- Chromium 真实点击：彩虹配色下拉可打开，选择“晨曦”后自动启用并更新当前配色。
- Chromium 真实点击：曲线、直线、折线、圆角四项均可切换；折线关闭并重开样式面板后仍保持选中。
- Chromium 真实点击：创建待办、打开大纲、切换未完成/已完成并分别悬停 900ms；只显示紧凑方块，没有生成遮挡内容的大尺寸预览。
- 本轮浏览器控制台为 0 error；修改已同步思源运行目录，但仍未 commit、push 或更新 Pages/Release。

### 2026-07-29 外壳、搜索与编辑几何增量验证

- 默认导航缩略图改为关闭状态，底栏保留明确的“显示缩略图”入口；桌面端和 390×844 移动端均通过。
- “已保存”改为透明静默状态；左上角品牌改用项目内 32/64/128px PNG 资源，插件与网页版同源。
- 搜索/替换面板完成视觉重排，并通过普通搜索、上下导航、正则非法提示、替换当前和全部替换的真实 Chromium 回归。
- 修复“替换当前”触发异步重绘后紧接“全部替换”失效的问题；新增同一流程连续重复 3 轮的桌面端和移动端稳定性验证。
- 带图标或待办节点的编辑框预留附件空间；根节点完成编辑时强制刷新最终文本渲染，避免附件遮挡与末尾裁切。
- 大纲分支线按三角标记中心对齐，不再额外左移 1px；待办悬停继续保持紧凑提示。
- 思源 3.7.3 冷启动后实机确认新品牌、静默保存、默认隐藏缩略图、搜索面板和大纲分支显示；运行目录 809 个文件与候选构建一致。
- 当前候选 `index.js` SHA-256 为 `C6B38BC9E5BCDE7DB0EA597B6B47774DAADE9A76D33018FD608B6713B03E1B31`，`index.css` 为 `8F886FC8038B8999E9DF49ADA99DD3F03650C2BACA254102D89556942F87BC80`。
