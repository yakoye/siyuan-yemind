# YeMind v0.6.4 实施计划

日期：2026-07-20

## Task 1：查找/替换披露状态

- 新增 `src/editor/searchPanelState.ts`。
- 修改 `src/editor/editorTemplate.ts`、`src/editor/YeMindEditor.ts` 和搜索样式。
- 先写 `v064SearchPanel*.test.ts`，验证默认查找、展开替换和 DOM/输入稳定性。

## Task 2：叶子节点标识与拖拽区

- 修改 `src/editor/outline.ts`、`src/editor/outlineDrag.ts`、`src/editor/YeMindEditor.ts` 和大纲样式。
- 先验证叶子标识条件及 `clientX < textLeft` 的拖拽边界。
- 保留编辑区、Quill、分支按钮的事件隔离。

## Task 3：思源全局搜索

- 新增 `src/plugin/globalSearch.ts`。
- 修改 `YeMindZenPlugin.ts`、`OpenMapTabRegistry.ts`、`tabs.ts` 和 `host.ts`。
- 测试标题、正文、备注、批注等匹配、陈旧结果清除和点击定位。

## Task 4：整图样式

- 新增 `src/editor/projectStyle.ts`、`src/ui/projectStylePanel.ts`。
- 修改模板、菜单、编辑器外观应用、数据模型、MapRepository 和 CheckpointRepository。
- 测试密度、彩虹线、背景色规范化、面板事务、仓库和检查点持久化。

## Task 5：视觉与兼容回归

- 增加项目样式面板、叶子圆点、搜索结果和查找披露 CSS。
- 顶部“样式”和节点右键“节点样式”使用不同图标和数据范围。
- 更新旧工具栏契约测试，继续运行全部历史回归。

## Task 6：发布

执行：

```bash
npm test
npm run check
npm run build
node --check index.js
npm audit --omit=dev
```

打包并验证：

```bash
zip -r /mnt/data/siyuan-yemind-zen-v0.6.4.zip . -x 'node_modules/*' 'dist/*' '.git/*' '*.zip'
unzip -t /mnt/data/siyuan-yemind-zen-v0.6.4.zip
```

完整解压后重新执行 `npm ci`、测试、类型检查、构建和入口语法检查。
