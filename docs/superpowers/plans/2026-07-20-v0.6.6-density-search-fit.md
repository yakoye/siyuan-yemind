# YeMind v0.6.6 Density, Search Navigation and Fit Icon Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remap density presets, compact the whole-map style panel, make global-search results reliably open maps, and replace the fit-view glyph with the requested SVG.

**Architecture:** Keep project appearance in the existing `ProjectStyle` adapter and preserve `simple-mind-map` as the rendering authority. Fix search activation at the DOM event boundary rather than introducing mirrored SiYuan blocks. Reuse the existing toolbar SVG system for the new fit icon.

**Tech Stack:** TypeScript, simple-mind-map, SiYuan Plugin API, Vitest/jsdom, Vite.

---

### Task 1: Density semantics

**Files:**
- Modify: `src/editor/projectStyle.ts`
- Modify: `src/ui/projectStylePanel.ts`
- Test: `tests/v066DensityPanelSearchFit.test.ts`
- Test: `tests/v065ProjectDensity.test.ts`
- Test: `tests/v064ProjectStyle.test.ts`

- [x] Write failing assertions for compact 30/2, default old-compact, and comfortable old-default.
- [x] Run the focused test and confirm the old values fail.
- [x] Update `densitySpacing()` with the requested mappings.
- [x] Make custom inputs display the selected preset's node spacing instead of stale constants.
- [x] Run focused and historical density tests.

### Task 2: Compact style panel

**Files:**
- Modify: `src/editor/editorTemplate.ts`
- Modify: `src/styles/index.css`
- Test: `tests/v066DensityPanelSearchFit.test.ts`

- [x] Add a failing test that rejects density subtitles and requires a 220px panel.
- [x] Remove density `<small>` elements and section description paragraphs.
- [x] Compress density controls, spacing inputs, colors, switch, footer and header.
- [x] Keep outside-click dismissal and readonly behavior unchanged.
- [x] Run project style tests.

### Task 3: Reliable global-search activation

**Files:**
- Modify: `src/plugin/globalSearch.ts`
- Test: `tests/v066DensityPanelSearchFit.test.ts`
- Test: `tests/v064GlobalSearchMount.test.ts`
- Test: `tests/v065GlobalSearchNavigation.test.ts`

- [x] Add a failing mousedown activation test.
- [x] Activate on primary-button mousedown before the host can remove blurred results.
- [x] Retain click and Enter activation.
- [x] Deduplicate the click that follows mousedown for 500ms.
- [x] Confirm existing pending-node navigation remains green.

### Task 4: Fit-view SVG

**Files:**
- Modify: `src/editor/projectControls.ts`
- Modify: `src/editor/editorTemplate.ts`
- Test: `tests/v066DensityPanelSearchFit.test.ts`

- [x] Add a failing assertion for `.ymz-icon-fit-view`.
- [x] Implement four-corner and center-circle SVG with `currentColor`.
- [x] Replace the bottom `⌖` glyph while retaining the `fit` action and accessible label.
- [x] Run toolbar tests.

### Task 5: Release verification

**Files:**
- Modify: `package.json`
- Modify: `package-lock.json`
- Modify: `plugin.json`
- Modify: `src/plugin/constants.ts`
- Modify: `AGENTS.md`
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Modify: `README_zh_CN.md`
- Modify: `DEVELOPMENT_PLAN.md`
- Modify: `FEATURE_MATRIX.md`
- Modify: `MIGRATION_STATUS.md`
- Create: `docs/OFFICIAL_FEATURE_PARITY_v0.6.6.md`
- Create: `docs/verification-v0.6.6.md`

- [ ] Set all release identities to 0.6.6.
- [ ] Run `npm test` and record file/test counts.
- [ ] Run `npm run check`.
- [ ] Run `npm run build` and record transformed modules.
- [ ] Run `node --check index.js`.
- [ ] Run dependency audit without modifying dependencies.
- [ ] Create the release ZIP excluding `node_modules`, `dist`, `.git` and nested ZIPs.
- [ ] Run `unzip -t`.
- [ ] Extract to a clean directory, run `npm ci`, tests, type check, build and entry syntax check again.
