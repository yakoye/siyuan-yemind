# YeMind Zen v0.5.21 实施计划

1. 先为 Root 持久化、大纲往返、节点选择显示矩阵和数字点击路由编写失败测试。
2. 删除启动阶段 Root 强制展开迁移。
3. 让 `flattenOutline` 和 Root disclosure 使用真实 `expand`。
4. 将快捷控制改为“折叠数字优先、选中展开节点显示操作”。
5. 将 `+ / −` 统一为当前 `−` 的视觉规格。
6. 运行相关测试、完整测试、TypeScript、构建、入口语法、ZIP 和解压后二次验证。
