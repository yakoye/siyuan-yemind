# YeMind v0.6.1 实施计划

日期：2026-07-20

## Task 1：版本与失败测试

修改/新增：

- `package.json`
- `plugin.json`
- `src/plugin/constants.ts`
- `tests/v061OutlineSelection.test.ts`
- `tests/v061ToolbarLayout.test.ts`
- `tests/v061NodeDialogs.test.ts`
- `tests/v061Branding.test.ts`
- `tests/userFeedbackRegressionMatrix.test.ts`

步骤：

1. 先写活动 Quill 正文不能启动拖拽的失败测试；
2. 写工具栏入口唯一性的失败测试；
3. 写备注/批注内容区关闭按钮的失败测试；
4. 写品牌绿色的失败测试；
5. 确认测试因当前实现不符合需求而失败；
6. 更新版本为 0.6.1。

## Task 2：大纲文字选择与整行拖拽隔离

修改：

- `src/editor/outlineDrag.ts`
- `src/editor/YeMindEditor.ts`
- `src/styles/index.css`

步骤：

1. 扩展拖拽启动上下文，明确活动编辑器和可编辑目标；
2. `.ql-editor`、`[contenteditable=true]`、活动 `.is-editing` 宿主永不启动结构拖拽；
3. 非编辑行正文继续使用长按 + 移动启动拖拽；
4. 强制大纲编辑器 `user-select:text`、`touch-action:auto`；
5. 运行专项测试与既有大纲拖拽测试。

## Task 3：工具栏重排与入口功能

修改：

- `src/editor/editorTemplate.ts`
- `src/editor/projectControls.ts`
- `src/editor/YeMindEditor.ts`
- `src/styles/index.css`
- `tests/editorTemplate.test.ts`

步骤：

1. 移除顶部检查点、撤销、重做、+子、+同；
2. 线型增加“线型”文字；
3. 增加节点样式图标 + 文字按钮；
4. 左侧移除适配和重置，将原重置位置改为“历史”；
5. 底部移除搜索；
6. `node-style` 调用现有面板；
7. `checkpoints` 左侧入口调用现有检查点菜单；
8. 验证工具栏可用性和只读状态。

## Task 4：备注/批注关闭按钮

修改：

- `src/ui/nodeContentDialogs.ts`
- `src/styles/index.css`
- `tests/v061NodeDialogs.test.ts`

步骤：

1. 两类 Dialog 隐藏宿主关闭图标；
2. 添加内容区标题栏和关闭按钮；
3. 绑定关闭行为；
4. 隐藏两类弹窗的宿主标题栏；
5. 验证保存、取消和即时批注持久化不受影响。

## Task 5：品牌颜色与永久回归矩阵

修改：

- `src/styles/index.css`
- `tests/v061Branding.test.ts`
- `tests/userFeedbackRegressionMatrix.test.ts`

步骤：

1. YeMind 和导图标题使用 `--ymz-green`；
2. 将用户历次高频问题集中为永久契约测试；
3. 运行所有相关测试。

## Task 6：文档、全量验证与打包

修改：

- `AGENTS.md`
- `CHANGELOG.md`
- `DEVELOPMENT_PLAN.md`
- `FEATURE_MATRIX.md`
- `MIGRATION_STATUS.md`
- `README.md`
- `README_zh_CN.md`
- `docs/OFFICIAL_FEATURE_PARITY_v0.6.1.md`
- `docs/verification-v0.6.1.md`

验证：

```bash
npm test
npm run check
npm run build
node --check index.js
npm audit --omit=dev
```

打包并从最终 ZIP 重新验证：

```bash
zip -r /mnt/data/siyuan-yemind-zen-v0.6.1.zip . -x 'node_modules/*' 'dist/*' '.git/*' '*.zip'
unzip -t /mnt/data/siyuan-yemind-zen-v0.6.1.zip
```

完整解压后执行：

```bash
npm ci
npm test
npm run check
npm run build
node --check index.js
```
