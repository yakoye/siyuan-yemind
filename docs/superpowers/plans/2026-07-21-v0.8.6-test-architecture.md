# YeMind v0.8.6 测试体系重构实施计划

1. 记录 v0.8.5 基线：159 个入口、448 项测试、完整通过。
2. 审计每个测试的功能域、源模块和历史回归价值。
3. 删除精确重复身份/版本断言。
4. 将场景移动到 `tests/suites/<domain>` 并改名为 `.suite.ts`。
5. 创建 15 个 `tests/specs/<domain>.test.ts` 入口。
6. 添加 `suite-manifest.json` 和 `check-test-structure.mjs`。
7. 添加测试架构说明与覆盖矩阵。
8. 执行 445 项测试、TypeScript 和生产构建。
9. 生成平铺覆盖 ZIP，从 ZIP 解压后二次执行完整门禁。
