# YeMind v0.8.5 实施计划

1. 解析用户诊断包并确认错误事件顺序。
2. 写失败测试：旧大纲焦点在画布结构变化后不得恢复。
3. 新增 `EditingSurfaceCoordinator`，实现所有权与代次票据。
4. 接入画布 pointerdown、node_active、before_show_text_edit 和视图切换。
5. 删除 `renderOutline()` 中根据活动编辑器自动生成恢复请求的逻辑。
6. 将大纲结构命令统一改为显式 `queueOutlineFocus()`。
7. 增加所有权切换诊断事件。
8. 执行全量测试、TypeScript、生产构建和 ZIP 解压后二次验证。
