# YeMind Zen v0.5.23 实施计划

日期：2026-07-20

1. 添加真实 SVG/Quill 集成失败测试。
2. 添加 window mouseup 结束右键拖动失败测试。
3. 添加统一用户反馈回归矩阵。
4. 修复 `CanvasRightDragController` 的全局收尾和公开 cancel。
5. 修复富文本编辑事件隔离、可选择 CSS 和 editable-target 判断。
6. 在 `before_show_text_edit` 清理残留拖动。
7. 跑完整测试、TypeScript、生产构建和入口语法检查。
8. 打包后完整解压，再次安装锁定依赖并重复完整验证。
