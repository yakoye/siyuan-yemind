# YeMind v0.8.4 实施计划

1. 从用户诊断包复现 `opened → repositioned(-6,-4)` 失败链。
2. 新增富文本几何解析模块，拒绝零矩形并支持 SVG 屏幕矩阵恢复。
3. 将画布富文本编辑事务绑定到节点 UID，所有阶段解析当前渲染实例。
4. 覆盖更新与提交逻辑，不再调用会读取零矩形的上游重定位实现。
5. 写入失败测试：同位拖动后零矩形、失效节点引用、矩阵恢复、提交到当前节点。
6. 保留并运行全部历史编辑、拖动、大纲、搜索、存储和诊断回归。
7. 类型检查、生产构建、入口语法、ZIP CRC 和解压后二次验证。
