# YeMind v0.9.14 implementation plan

1. Add one combined-summary planner and route the command adapter through it.
2. Replace direct-body partial rerender measurement relocation with a styled off-screen host and one full geometry repair.
3. Cancel upstream right-button selection when drag-first pan begins.
4. Add domain-aligned permanent tests and one real Chromium regression.
5. Update all version surfaces, build the overlay without fixed assets, extract it and repeat validation.
