# YeMind Zen v0.6.3 实施计划

日期：2026-07-20

## Task 1：版本和设计契约

修改：

- `package.json`
- `plugin.json`
- `src/plugin/constants.ts`
- `AGENTS.md`

测试：`tests/pluginIdentity.test.ts`

先将断言改为 0.6.3 并确认失败，再统一版本。

## Task 2：选择/拖动图标和设置页

修改：

- `src/editor/projectControls.ts`
- `src/editor/selectionPresentation.ts`
- `src/editor/editorTemplate.ts`
- `src/settings/settingsDialogTemplate.ts`
- `src/settings/settingsDialog.ts`
- `src/styles/index.css`

新增测试：

- `tests/v063CanvasModeIcons.test.ts`

RED：

- 底部按钮包含箭头/手型 SVG，无“选/拖”正文；
- 设置页包含两个图标单选按钮；
- 状态切换同步更新图标和 aria-label。

GREEN：实现统一图标展示和设置绑定。

## Task 3：预览避让和批注时间

修改：

- `src/ui/nodeHoverPreview.ts`
- `src/ui/commentsPresentation.ts`
- `src/styles/index.css`

新增测试：

- `tests/v063HoverPreviewPlacement.test.ts`
- 扩展 `tests/v0517NodeNotes.test.ts`

RED：

- 右侧空间不足时选择左侧；
- 下方空间不足时选择上方；
- 最终矩形不与 anchor 相交；
- 批注预览包含格式化时间。

GREEN：实现候选定位算法和日期布局。

## Task 4：窄画布工具栏

修改：

- `src/styles/index.css`

新增测试：

- `tests/v063ResponsiveToolbars.test.ts`

RED：断言顶部/底部单行滚动、子项不收缩、窄屏隐藏次要状态。

GREEN：实现响应式 CSS。

## Task 5：紧凑备注和批注对话框

修改：

- `src/ui/nodeContentDialogs.ts`
- `src/ui/commentsPresentation.ts`
- `src/styles/index.css`

新增测试：

- `tests/v063NodeContentDialogs.test.ts`

RED：

- 对话框使用紧凑内部头部；
- 批注时间/操作位于卡片底部；
- 备注编辑区使用主体/底部结构；
- 窄屏宽度受限。

GREEN：实现模板和样式。

## Task 6：节点样式统一和菜单整理

修改：

- `src/ui/contextMenu.ts`
- `src/editor/projectControls.ts`
- `src/styles/index.css`

新增测试：

- `tests/v063ContextMenuOrganization.test.ts`

RED：

- 右键节点样式使用 `nodeStyleIcon()`；
- 菜单包含四个分组子菜单；
- 外层 overflow hidden，内部 items 单滚动；
- 删除操作仍直接显示。

GREEN：重排菜单且复用现有命令。

## Task 7：长期回归和发布

更新：

- `CHANGELOG.md`
- `DEVELOPMENT_PLAN.md`
- `FEATURE_MATRIX.md`
- `MIGRATION_STATUS.md`
- `README.md`
- `README_zh_CN.md`
- `docs/OFFICIAL_FEATURE_PARITY_v0.6.3.md`
- `docs/verification-v0.6.3.md`

验证：

```bash
npm test
npm run check
npm run build
node --check index.js
npm audit --omit=dev
```

打包并从 ZIP 二次验证：

```bash
zip -r /mnt/data/siyuan-yemind-zen-v0.6.3.zip . -x 'node_modules/*' 'dist/*' '.git/*' '*.zip'
unzip -t /mnt/data/siyuan-yemind-zen-v0.6.3.zip
```
