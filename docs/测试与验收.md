# YeMind 测试与验收

## 1. 质量阶段

1. 设计评审：核对双端共享边界、数据兼容、界面状态、错误处理和发布回滚。
2. 开发自测：每项行为先写失败测试，再做最小实现并运行相关领域测试。
3. 自动化测试：运行结构、语法、类型、主测试、网页测试、离线测试和资源检查。
4. 浏览器验收：使用 Chromium 覆盖桌面、小窗口和移动视口，保留失败 trace 与截图。
5. 思源验收：同步到指定运行目录，在真实思源宿主中打开旧图、新建图并验证保存重开。

任何核心用例失败、数据无法往返、运行目录版本不一致、发布包无法解压或线上资源 404，均阻断发布。

## 2. 导图矩阵

| 编号 | 前置条件 | 操作 | 预期 |
| --- | --- | --- | --- |
| MAP-CRUD-01 | 含根、中间、叶节点 | 新增父/同级/子节点，编辑并删除 | 数据正确，撤销和重做可恢复 |
| MAP-DRAG-01 | 至少两个分支 | 同父、跨父、跨侧拖动 | 子树完整，不能形成循环 |
| MAP-FOLD-01 | 含多级节点 | 单节点、同级、全部展开折叠 | 控件、数字和实际状态一致 |
| MAP-LAYOUT-01 | 含附件与折叠状态 | 逐一切换五种基础结构 | 节点 ID、文本、附件和折叠状态不丢失 |
| MAP-GEOMETRY-01 | 左右侧均含节点 | 缩放、编辑长文本、添加附件 | 文本、加减号和数字垂直居中且不重叠 |

## 3. 大纲矩阵

| 编号 | 前置条件 | 操作 | 预期 |
| --- | --- | --- | --- |
| OUTLINE-CRUD-01 | 大纲视图 | Enter、Tab、Shift+Tab、删除、编辑 | 层级和选区稳定，导图同步 |
| OUTLINE-DRAG-01 | 含多级节点 | 同层、跨层、跨分支拖动 | 预览和最终层级一致 |
| OUTLINE-GRIP-01 | 六点柄默认隐藏 | 指针接近左侧、键盘聚焦、触摸两次 | 按意图显示，命中区不引起跳动 |
| OUTLINE-ASSET-01 | 节点含图片或剪贴图 | 选中、八点缩放、替换、删除、预览 | 两端行为一致且可撤销 |
| OUTLINE-TEXT-01 | Markdown/OPML/TXT/MM 文本 | 导入并转成节点 | Unicode、层级和空行规则正确 |
| OUTLINE-MENU-01 | 非根节点 | 右键后依次执行编辑、删除当前与子级、仅删除当前 | 命令作用于右键目标，导图和大纲立即同步 |
| OUTLINE-TXN-01 | 大纲含根、中间、叶节点 | 普通输入、输入空格、输入数字、清空、行内格式 | 只更新受影响 UID；不替换整树、不改变节点数和画布缩放 |
| OUTLINE-CLIPBOARD-01 | 复制网页段落 | 粘贴含软换行的单段 HTML，再粘贴明确缩进大纲 | 单段只生成一个节点；明确层级才进入结构事务 |
| OUTLINE-DELETE-01 | 已选中文字且工具栏获得过焦点 | Delete/Backspace | 恢复保存选区并删除文字，不误删节点或失去焦点 |

## 4. 内容与附件矩阵

| 编号 | 前置条件 | 操作 | 预期 |
| --- | --- | --- | --- |
| CONTENT-MARKER-01 | 任意节点 | 插入、替换、删除标号和剪贴图 | 图形清晰，保存重开一致 |
| CONTENT-IMAGE-01 | 本地图片 | 插入、缩放、替换、删除、放大预览 | 无漂移，预览可用 Escape 关闭 |
| CONTENT-NOTE-01 | 任意节点 | 新增和编辑备注、批注、挖空/模糊 | 徽标与正文一致，重开不丢 |
| CONTENT-RELATION-01 | 两个节点 | 新建、编辑文字、调控制点、删除关联线 | 命中稳定，保存恢复一致 |
| CONTENT-FORMULA-01 | 选中文字 | 点击公式按钮并输入 | 公式图标可见且内容正确插入 |
| CONTENT-RICH-01 | 导图或大纲选中文字 | 逐一执行粗体、斜体、下划线、删除线、行内代码、文字/背景色、字号、字体、链接、代码块、模糊、公式和清除 | 点击不结束当前编辑；选区不丢；格式可见、可取消并保存 |
| CONTENT-TEXT-01 | 普通文本节点 | 进入编辑、提交、再次进入并原样提交 | 无格式内容不升级成富文本；编辑前后外框宽高差不超过 1px |
| CONTENT-COPY-01 | 导图富文本已有选区 | Ctrl/Cmd+C | 同时产生纯文本和 HTML；DOM 选区短暂丢失时仍使用编辑器保存选区 |
| CONTENT-EMPTY-01 | 空节点或仅空白节点 | 在导图和大纲输入、清空、切换焦点 | 统一保存为空字符串；节点外框不塌缩，快捷按钮不漂移 |

## 5. 外观、工具栏和菜单矩阵

| 编号 | 前置条件 | 操作 | 预期 |
| --- | --- | --- | --- |
| UI-APPEARANCE-01 | 两个宿主 | system/light/dark 逐一切换 | 外壳、编辑器、菜单和弹窗同步且清晰 |
| UI-BAR-01 | 自动隐藏模式 | 分别接近上、左、下边缘 | 任一边缘均唤回三条工具栏，共用延迟收起状态 |
| UI-BAR-02 | 三侧均隐藏 | 鼠标、触摸、Tab 操作细线边缘标记 | 标记接近 version47，用户可发现且可恢复全部工具栏 |
| UI-ZEN-01 | 任意工具栏状态 | 进入和退出禅模式 | 工具栏与边缘标记均隐藏，仅保留退出入口 |
| UI-MENU-01 | 四种视口边缘 | 打开节点、空白、大纲和选区菜单 | 菜单及子菜单不超出视口 |
| UI-CLASSIC-01 | 主题面板“经典”分组 | 检查九张主题卡并逐一应用 | 每张显示并使用六个真实分支色，颜色顺序与 v1.5 清单截图一致 |
| UI-DARK-01 | 深色宿主 | 打开主题、样式、搜索、大纲、卡片和复习面板 | 输入框、按钮、正文、悬停和选中态无白底刺眼或黑字不可读 |
| UI-SELECTION-01 | 节点与文字分别选中 | 检查节点选框和文字工具栏 | 节点无第二层填充/阴影；工具栏容器透明，按钮状态仍清晰 |
| UI-ANCHOR-01 | 分栏中从大纲选择多个节点 | 连续输入、清空并在行间切换 | `+ / − / 数字` 始终锚定 UID 对应的实时画布节点 |

## 6. 导入导出与生命周期矩阵

| 编号 | 前置条件 | 操作 | 预期 |
| --- | --- | --- | --- |
| IO-ROUNDTRIP-01 | 含完整节点内容 | 默认导出 `.yemind.svg` 后导入 | 完整数据无损恢复 |
| IO-FORMATS-01 | 格式样例 | 导入支持的 map/outline 文件并导出全部格式 | 自动识别正确，失败提示可理解 |
| STORE-01 | 编辑中的导图 | 快速连续保存、关闭、重开 | 新写入不会被旧写入覆盖 |
| STORE-FAIL-01 | 模拟存储失败 | 增删改 | 内存状态回滚，无半保存 |
| RELEASE-01 | 已验证 commit | 构建双端 ZIP、manifest、SHA256 | 包结构与哈希全部匹配 |

## 7. 性能与健壮性

- 工具栏和六点柄动画只改变 `opacity` 与 `transform`，不触发编辑区重排。
- 拖动和缩放期间不得出现持续长任务、未捕获异常或明显输入丢失。
- 图片加载失败、损坏导入、存储失败和只读拒绝必须保留可继续使用的编辑器。
- 自动化不得用固定长等待掩盖竞态，应等待明确的 DOM、保存或渲染状态。

## 8. 验收证据

发布验收记录必须包含测试命令、通过数量、跳过原因、浏览器视口、运行目录哈希、发布物大小与 SHA256、GitHub Actions 结果、Release 链接和 Pages 链接。失败时保存日志、trace、截图及最小复现步骤。

回滚时撤销发布标签和 Pages 部署，恢复上一稳定 Release；用户地图数据不参与插件文件同步或清理。

## 9. 复习与卡片

v1.5.0 已包含卡片和复习的首个可用版本，验收范围包括从当前节点创建卡片、编辑正反面、搜索与状态筛选、收藏、翻面、三档复习评价、进度持久化和保存失败提示。后续版本可继续改进算法和批量工作流，但不得把这些基础能力回退为占位界面。
