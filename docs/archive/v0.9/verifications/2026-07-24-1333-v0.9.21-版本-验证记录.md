# YeMind v0.9.21 verification

Verification covers test-structure validation, TypeScript syntax parsing, offline behavior entries, deterministic offline bundle generation, real Chromium source-icon and double-click-toolbar interaction, prior browser regression entries, ZIP CRC, full extraction, and extracted-package reruns.

Dependency-backed Vitest, strict TypeScript and Vite results are reported only when `npm ci` succeeds. Unavailable checks are listed explicitly rather than represented as passed.
