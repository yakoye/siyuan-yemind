# YeMind v0.9.7 product boundaries

v0.9.7 changes only right-growing `logicalStructure` canvas target resolution and its drag preview. It does not change persisted map, settings or checkpoint schemas. Other layout families retain their existing geometry adapters. The release preserves the v0.9.6 outline editing model, image tools, theme model, repository format and structural command path.
