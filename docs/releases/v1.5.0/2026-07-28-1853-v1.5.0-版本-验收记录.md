# YeMind v1.5.0 验收记录

## 范围

- 界面：version47 外壳、三边工具栏、响应式“更多”、明暗外观。
- 核心：28 种结构、节点快捷控件、导图/大纲编辑拖动、富文本与资源。
- 新增：卡片面板、三档复习队列、6 个基础主题。
- 传输：v1.5.0 后缀、双 HTML、旧格式兼容。
- 双端：思源插件和独立网页版同源构建。

## 已执行结果

| 项目 | 结果 |
|---|---|
| 版本一致性 | `1.5.0`，8 个标记一致 |
| TypeScript | `tsc --noEmit` 通过；443 个 TypeScript 文件语法检查通过 |
| 核心单元/集成 | 15 个测试域、246 个场景模块；802 项通过，45 项按既有条件跳过 |
| 网页适配测试 | 6 个测试文件、20 项通过 |
| 离线冒烟 | 17 组主题、外观事务、大纲、拖动、资源、导入等冒烟入口全部通过 |
| 固定资源 | 126 个标号、764 个剪贴图、28 个结构及全部目录资源通过 |
| 真实浏览器 | Chromium 桌面与 390×844 移动端共 26 项通过，2 项按项目限定视口条件跳过 |
| 插件构建/同步 | 生产构建通过；809 个运行文件同步到思源插件目录 |
| 思源实机 | SiYuan 3.7.3 加载通过；202 节点旧导图、大纲、卡片、主题、视图和深色画布切换通过 |
| 网页构建/Pages | 网页生产构建通过；公开 Pages 为 v1.5.0，成品页面和控制台复核通过 |
| Release 包与 SHA-256 | 2 个 ZIP、发布清单和 `SHA256SUMS` 校验通过；两个 ZIP 总计 7,474,269 字节 |

## Version47 二次对齐验收

- 顶部响应式分隔线始终为 1px，不再出现暗色灰色方块；品牌、导图、跟随系统和全屏图标按确认稿统一。
- 缩略图的可视区域框经过边界归一化，负数、非有限值和过度约束不会再生成错误灰条。
- 三边隐藏提示改为随浅色/深色变化的 2px 细线；鼠标停留在热点超过隐藏延时后仍保持工具栏展开。
- 大纲分支的三角、方块和分段导线使用同一稳定分支色；当前行和文字选择在两种外观下均可读。
- 搜索与替换面板具有 Aa、全字、正则、选中范围和 AB 控制；同节点多处命中逐项导航，非法正则显示错误，富文本替换保留标签结构且不跨段误匹配。
- 新建节点输入在当前帧周期内可见；删除或结构变化会使排队中的旧编辑帧失效，避免文字与节点框分离。
- 思源标签在异步数据加载前完成注册，同 map ID 的恢复标签、点击打开和并发请求复用同一标签。
- Chromium 实测浅色、暗色、大纲、搜索、缩略图、自动隐藏、768px 与 390px；控制台 0 error、0 warning。

## 发布物

| 文件 | 字节 | SHA-256 |
| --- | ---: | --- |
| `siyuan-yemind-v1.5.0.zip` | 3,842,889 | `d3fbdc825828ac743f23416b2d16731a383d23552e1abb2a58d37646561800a9` |
| `yemind-web-v1.5.0.zip` | 3,631,380 | `72920d052cbbffadfaad1423a08ba34b65389178d2eb60773add513c283b719f` |

本表记录 Version47 二次对齐候选构建；正式发布使用同一提交重新生成并以公开 `release-manifest.json` 和 `SHA256SUMS` 为最终依据。

## 关键人工结论

- 编辑器宽度而不是浏览器总宽度决定顶部收纳，网页版导图库不会再挤掉右侧操作。
- 结构面板顺序符合清单，组织结构为向下、向上、向右。
- 基础主题为默认、墨枝、质感、极光、晨雾、沙丘；新主题含独立六色浅/深定义。
- 经典九套主题各显示并实际使用六种分支色；“永恒”实机连线与截图六色完全一致。
- 缤纷和经典主题在不改变主题 ID 的前提下生成深色画布及可读节点层级，不再出现深色外壳配白色画布。
- 卡片侧栏和全屏模式可读，深色和窄屏下进度、筛选与空状态清晰。
- 菜单和浮层保持在视口内，键盘和触摸长按路径保留。
- 390×844 网页版导图库按钮与“导图”按钮不再重叠，并有浏览器回归断言。
- 思源必须冷启动才能清除旧插件模块缓存；普通页面刷新不足以验证新构建。
- 运行目录 `index.js`、`index.css`、`plugin.json`、`VERSION` 与最终本地构建 SHA-256 一致；同步未触碰思源用户地图存储。

## 代码审查闭环

- 插件导入与网页导入都保留 `studyCards`，失败时删除半创建文档。
- 卡片快速连续写入使用串行队列；只读模式在界面和事件入口双重拒绝写入。
- 卡片/复习切换清理图片八点框、资源浮层、富文本工具栏、预览和搜索面板。
- 大纲图片单击打开统一资源操作，双击进入放大预览，不再打开旧图片地址表单。
- 当前筛选卡片作为显式复习队列，不会被“全部到期卡片”覆盖。
- 旧 `organization-bidirectional` 与 `yemindOrganizationBidirectional` 显式迁移为 `organization-right`。
- ZIP 导入在读取条目内容前限制条目数、单项大小、累计解压大小、压缩比和路径。
- 顶部/底部更多菜单支持方向键、Home、End、Esc，且属于自动隐藏交互区域。

## 发布阻断条件

若运行目录同步删除 `data/`、任一自动门禁失败、Pages 与最终提交不一致、Release 缺少任一 ZIP/清单/哈希，或思源实际加载版本不是 1.5.0，则本记录不能标记为最终通过。

## 已知上游依赖事项

`npm audit` 在 2026-07-28 报告 0 个 critical、10 个 high、1 个 moderate、1 个 low。High 主要来自 vendored `simple-mind-map` 将 ESLint/Tern/Glob 工具链列入运行依赖；浏览器代码未调用这些工具路径。UUID 告警涉及带外部 buffer 的 v3/v5/v6，当前仅调用 v4；Quill 2.0.3 的低风险项涉及 HTML 导出。强制修复会跨主版本替换导图引擎工具链，因此不在 v1.5.0 中盲目执行，后续按 `DEVELOPMENT_PLAN.md` 在保持行为兼容的前提下单独升级。

## 2026-07-29 本地候选增量复验

本轮修改尚未提交、推送或重新发布，不能用本节替代公开 Pages/Release 验证。

- 核心测试更新为 845 项通过、45 项条件跳过；网页测试 20 项通过；浏览器 E2E 28 项通过、2 项条件跳过。
- 修复富文本首屏重复节点、搜索上一项计数滞后、图片选中层在拖动画布和外层布局变化后脱离图片的问题。
- 图片选择层改用浏览器实时绘制矩形，并在选中期间持续跟随；真实思源窗口完成半屏、全屏、拖动画布和大纲分栏复验。
- 13 种导出产物实际生成并检查；11 类可恢复格式通过网页版真实文件选择器导入。
- 主题卡改为“单击即时预览、应用后保存、关闭未应用则恢复”；思源实机和 Chromium 均验证。
- 彩虹连线重构为单行组合控件；配色下拉、自动启用及状态更新通过 Chromium 实点验证。
- 线型补齐曲线、直线、折线、圆角四项；折线持久化及四项逐项切换通过。
- 大纲待办不再调用通用大尺寸悬浮预览；未完成、已完成状态切换和 900ms 悬停均未遮挡内容。
- 缩略图默认关闭但保留底栏入口；“已保存”去除绿色实心背景；品牌使用项目 32/64/128px PNG 资源。
- 搜索/替换重新排版并完成实际功能回归；连续“替换当前→全部替换”在桌面端、移动端各重复 3 轮均通过。
- 编辑框为待办、图标和标号预留附件空间；根节点结束编辑后刷新最终文本尺寸，避免遮挡和裁切。
- 大纲彩色三角与分支线改用同一中心横坐标；思源冷启动后对品牌、保存、缩略图、搜索面板和大纲显示完成实机复核。
- 完整证据见 [../dev/2026-07-29-1055-dev-记录-2026-07-29-验证记录.md](../dev/2026-07-29-1055-dev-记录-2026-07-29-验证记录.md)。
