# YeMind v1.2.0 多格式导入导出验证

验证日期：2026-07-27

## 自动化结果

- `npm test`：15 个测试文件通过，671 项通过，45 项按既有条件跳过。
- `npm run test:web`：5 个测试文件通过，16 项通过。
- `npm run check`：TypeScript 类型检查通过。
- `npm run test:syntax`：409 个 TypeScript 文件语法检查通过。
- `npm run check:version`：8 个发布版本标记均为 `1.2.0`。
- `npm run test:offline`：主题、大纲、拖动、资源、暗色模式和交互回归检查通过。
- `npm run verify:assets`：固定本地资源完整。
- `npm run build`：思源插件生产构建通过。
- `npm run build:web`：独立网页版生产构建通过。
- `node --check index.js`：插件发布入口语法检查通过。
- `git diff --check`：无空白错误，仅报告 Windows 行尾提示。

## 浏览器实测

- 独立网页版打开后控制台错误和警告均为 0。
- 导出弹层按需求显示 12 种格式，默认聚焦 `.yemind.svg`。
- 下载 `.yemind.svg` 成功，文件包含 `yemind-document` 恢复数据。
- 将下载文件重新导入后创建新导图，原导图未被覆盖，内容正常渲染。
- 在 `390 × 844` 窄屏下导出弹层可滚动，按钮和格式说明未溢出屏幕。

## 兼容性结论

- YeMind SVG、YeMind ZIP、PNG 附加包均可无损恢复完整 `YeMindMapDocument`。
- XMind 使用浏览器原生 ZIP/XML/JSON 编解码，不再引入 Node `stream` 依赖警告。
- 可读取参考插件的 `kmind-document-zip`、`kmindz-docs` SVG 元数据及 PNG `kmNd` 包。
- 自动导入兼容旧 `.yemind/.kmind/.json` 和 `.md/.opml/.txt/.mm` 大纲文件。

## 运行目录

- 已同步到 `D:\myDatabase\SiYuan\data\plugins\siyuan-yemind`。
- 运行目录 `VERSION` 与 `plugin.json` 均为 `1.2.0`。
- `index.js`、`index.css`、清单、版本、图标、许可证和说明文件的 SHA-256 均与发布工作树一致。
- 运行目录只保留发布白名单内容；未残留同步或备份临时目录。
