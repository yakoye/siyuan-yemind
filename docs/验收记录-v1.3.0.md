# YeMind v1.3.0 验收记录

验收日期：2026-07-27  
验收分支：`agent/yemind-web-runtime`  
目标宿主：SiYuan 3.7.3、独立网页版、GitHub Pages

## 1. 结论

v1.3.0 的本地代码、双端构建、运行目录和真实思源宿主验收通过。GitHub CI、Release 与 Pages 结果在推送后补录；线上任一工作流失败均继续阻断发布完成。

“复习与卡片”不在本版范围；本版只验证现有挖空/模糊功能。

## 2. 静态与单元门禁

| 门禁 | 实际结果 |
| --- | --- |
| `npm run check:version` | 8 个版本标记均为 `1.3.0` |
| `npm run test:structure` | 15 个领域、238 个场景模块 |
| `npm run test:syntax` | 427 个 TypeScript 文件通过 |
| `npm run check` | TypeScript 类型检查通过，0 错误 |
| `npm test` | 15 个测试文件；730 通过、45 跳过、0 失败 |
| `npm run test:web` | 6 个测试文件；19 通过、0 失败 |
| `npm run test:offline` | 18 组离线冒烟全部通过 |
| `npm run verify:assets` | 126 个标号、764 个剪贴图及固定资源完整 |

45 个单元跳过项是保留的历史宿主 DOM/真实渲染条件用例；相同核心行为由当前单元场景、18 组离线冒烟和真实浏览器套件覆盖。桌面专属的移动侧栏用例在桌面项目跳过，并在移动项目通过。

## 3. 真实浏览器验收

命令：`npm run test:e2e`

- Chromium 桌面视口：1366 × 768。
- Chromium 移动视口：Pixel 5。
- 结果：21 通过、1 个桌面不适用项跳过、0 失败。
- 覆盖：建图、重命名、IndexedDB 恢复、导图/分屏/大纲切换、明暗外观、六点拖动柄、三侧工具栏隐藏标记、菜单视口夹紧、完整导入导出入口。
- 富文本覆盖：导图和大纲的粗体、斜体、下划线、删除线、行内代码、文字色、背景色、字号、字体、链接、代码块、公式、挖空/模糊、取消模糊和清除格式。
- 大纲操作覆盖：右键编辑作用于目标行，删除作用于目标子树，工具栏点击不丢失文字选区。

## 4. 双端构建与发布物

`npm run release:build` 与 `npm run release:verify` 均通过。固定 ZIP 内文件时间后间隔 3 秒连续打包两次，两个 ZIP 的 SHA-256 完全一致。Vite 报告单包大于 500 kB 的性能提示，不影响构建和运行；思源要求单入口插件包，网页版后续可独立评估按需加载。

| 产物 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `siyuan-yemind-v1.3.0.zip` | 3,835,902 | `bc31ebb72d125a39d4336ed34e33f8edf59156bba36a0dc68a9d86c68f164872` |
| `yemind-web-v1.3.0.zip` | 3,609,601 | `46ed950bc1c10970727ff71d1869641c20216d1f90e594f4b85f691f5f8eee57` |
| `release-manifest.json` | 531 | `2a565366fb09e88b90b3c58eca40522188312349eeaf7b8d18cb101353739559` |

校验内容包括：清单版本、字节数、SHA-256、插件根文件、`assets/`、`i18n/`、网页版 `index.html`、`VERSION`、`.nojekyll` 与 `icon.png`。

## 5. 思源运行目录

目标路径：

```text
D:\myDatabase\SiYuan\data\plugins\siyuan-yemind
```

思源进程正在使用目录，因此采用 `npm run sync:runtime:in-place`。同步器先校验精确目标，再清理白名单外文件；同步前目标没有 `data/`，同步后仍没有创建或删除用户数据目录。

- 文件数：810。
- 运行版本：1.3.0。
- 文件名清单与构建白名单：完全一致。
- 810 个文件逐项 SHA-256：与仓库构建源完全一致。
- `plugin.json`：`b9bf1e615209dd83e1c588e26bbd745f0395db5b48fd003fd207f61007323046`
- `index.js`：`a8ab1f1f8a8a13b020a48a8d5bbfb8590cb5a27563314523ea28859e4ce8e911`
- `index.css`：`4e2b0ca9c3c5e8e9db5a685f9d1ca778381eb0aa6da5accdd2f7d33bda92da1a`

## 6. 真实思源宿主冒烟

本机 SiYuan Kernel 在端口 6806 返回 HTTP 200，桌面页面标题为 SiYuan。

- YeMind 顶部入口、停靠栏和编辑器工具栏正常加载。
- 打开已有“测试导图”：数据为 202 个节点。
- 切到大纲：投影 202 行，当前视口 24 行可见，无编辑器错误状态。
- 切回导图：视图状态恢复为 `map`，当前渲染 48 个节点。
- 关于页：当前版本、运行时代码版本、构建版本均为 1.3.0，构建标识为 `yemind-v1.3.0-20260727`。
- YeMind 过滤后的控制台错误：0。
- 宿主有 1 条 `siyuan-plugin-open-md` 的 `window.require is not a function` 错误，来源是其他插件，不属于 YeMind。

## 7. GitHub 与线上验收

推送后补录：

| 项目 | 状态 | 链接 |
| --- | --- | --- |
| GitHub CI | 待运行 | `https://github.com/yakoye/siyuan-yemind/actions/workflows/ci.yml` |
| GitHub Pages | 待部署 | `https://yakoye.github.io/siyuan-yemind/` |
| GitHub Release v1.3.0 | 待发布 | `https://github.com/yakoye/siyuan-yemind/releases/tag/v1.3.0` |

## 8. 回滚

线上异常时停止新增提交与标签，保留 Actions 日志和 Playwright trace；插件运行目录恢复上一稳定 Release 的白名单文件但不动 `data/`，网页版重新部署上一稳定标签。
