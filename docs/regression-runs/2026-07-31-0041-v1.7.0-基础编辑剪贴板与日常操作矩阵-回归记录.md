# YeMind v1.7.0 基础编辑、剪贴板与日常操作矩阵回归记录

> 基线清单：`docs/standards/回归验收清单.md` 1.1
> 创建时间：2026-07-31 00:41（Asia/Shanghai）
> 基线提交：`8cfc9e6`
> 候选提交：发布标签 `v1.7.0` 指向的提交；精确 SHA 由最终 `release-manifest.json` 记录
> 执行档位：发布级

## 变更登记

| 项目 | 内容 |
|---|---|
| 版本 | 1.7.0 |
| 版本判定 | Minor：在兼容现有导图格式的前提下，新增统一复制意图、跨表面/跨文件资源剪贴板和历史回放事务，属于主要编辑子系统重构 |
| 核心目标 | 导图与大纲中的文字、图片和节点结构复制粘贴语义一致；编辑、删除、拖动、折叠、撤销与重做形成稳定事务 |
| 受影响模块 | 导图画布、大纲、富文本、选区、剪贴板、图片资源、节点结构、历史、插件版与网页版 |
| 最近同类参考 | YeMind 既有 `RichTextToolbar`、节点剪贴板事务和图片资源选择器；KMind 仅用于研究事务边界，不复制其界面 |
| 复用方式 | 扩展既有共享控制器和命令适配器；未建立插件版/网页版平行实现 |
| 共享核心 | 是 |
| 数据格式 | 不变；跨文件粘贴重新生成 UID 并过滤整节点展示样式 |
| 视觉影响 | 无新增视觉组件；保留 v1.6.x 编辑层、工具栏和快捷控件基线 |
| 交互影响 | 是 |

## 根因与架构修复

1. 导图、大纲、右键菜单和键盘复制分别推断复制对象，旧节点载荷可能在真实文字复制后残留，导致后续粘贴错误变成整节点。
2. 图片选择、文字选区和节点选择缺少统一优先级；直接右击图片时旧选区可能抢占复制动作。
3. simple-mind-map 的延迟历史写入无法在立即撤销前可靠提交；历史回放触发的异步渲染回调还可能新建历史并截断重做分支。
4. 原有测试分散验证单个入口，没有把编辑态/非编辑态、导图/大纲、同文件/跨文件、新建节点前后组合成完整矩阵。

对应修复：

- 由统一复制意图决策层按“直接右击资源 → 真实文字选区 → 已选资源 → 节点结构”选择载荷。
- 图片剪贴板同时写入可用的二进制图片、安全 HTML、纯文本后备和 YeMind 资源元数据。
- 节点结构使用同一版本化载荷跨导图与大纲传递；跨文件过滤源 UID 与整节点展示样式。
- 历史协调器增加显式回放事务；从 BACK/FORWARD 开始到渲染结束期间拒绝新增历史，保证撤销/重做分支不被异步回调覆盖。
- 新建日常操作和剪贴板真实浏览器矩阵，逐帧检查编辑层、节点边框、工具栏、宽度拖动和快捷控件。

## 自动化证据

| 门禁 | 本次结果 |
|---|---|
| `npm run test:structure` | 通过：15 个领域，250 个场景模块 |
| `npm run check` | 通过：TypeScript 与文档校验退出码 0 |
| `npm test` | 通过：993 通过；45 个仓库既有环境跳过；无新增核心跳过 |
| `npm run test:web` | 通过：22/22 |
| `npm run test:offline` | 通过：全部离线冒烟输出为真 |
| `npm run verify:assets` | 通过：固定本地资源完整 |
| 基础编辑/剪贴板定向 E2E | 通过：51/51；快捷控件跟随场景另行连续重复 10/10 |
| 完整 Playwright（禁用重试） | 通过：85 实际执行、49 按桌面/移动端适用性跳过、0 失败、0 重试 |
| 版本、双端构建、发布包和哈希 | 待候选提交构建后补录 |

## 日常操作矩阵结论

| 范围 | 结论 |
|---|---|
| 新建 | `+`、Tab、Enter 的默认文字和首帧编辑位置通过 |
| 编辑 | 直接输入、双击输入、粘贴、清空、多行、宽度调整和节点切换通过 |
| 复制粘贴 | 导图/大纲、文字/图片/节点、编辑态/非编辑态、同文件/跨文件、新建节点前后全部通过 |
| 删除剪切 | Delete、Backspace、Ctrl+X、节点删除和右键删除一次生效 |
| 拖动 | 节点层级移动、父子树整体预览与提交、画布平移时快捷控件跟随通过 |
| 展开折叠 | 右键菜单、快捷控件和大纲状态同步通过 |
| 格式与工具栏 | 工具栏首个可见帧位置、全部直接格式控件和对话框动作通过 |
| 历史 | 文字、结构、图片、跨文件大纲粘贴均可撤销和重做，重做分支不再被截断 |

## 发布与实机

- [ ] v1.7.0 的 10 个版本标记一致。
- [ ] 插件版与网页版生产构建通过。
- [ ] 发布 ZIP、清单和 SHA-256 校验通过。
- [ ] 思源运行目录白名单同步且 `data/` 保留。
- [ ] 思源 3.7.3 完全退出后冷启动，核心流程实机通过。
- [ ] 独立网页版生产构建核心流程实机通过。
- [ ] GitHub CI、Release 与 Pages 通过并完成线上复验。

## 当前判定

- [ ] 可以发布
- [x] 自动门禁前半段通过，继续双端构建、实机、发布物与线上验证
- [ ] 不可发布
