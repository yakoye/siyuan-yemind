# YeMind v1.9.7 拖动预览首帧与候选父节点回归记录

> 后续实机纠正（2026-08-03 10:55 +08:00）：本记录关于“中间节点子树预览已通过”的结论无效。正式 `64aa8d70-clean` 中，叶子节点走上游单节点克隆而表现正常；中间节点会被 YeMind 替换为额外子树包装层，导致坐标系和包围盒语义改变，因此大多数中间节点仍会出现飞线与预览不一致。该错误设计在 v1.9.8 中删除，并由一级、深层中间节点矩阵重新验收。

## 1. 基线与范围

- 时间：2026-08-03 10:07 +08:00
- 回归基线：`docs/standards/回归验收清单.md` 1.1
- 分支：`stabilization/shared-text-layout`
- 修改前提交：`6e5a5b5`
- 发布版本：`v1.9.7`
- 执行档位：发布级
- Trace：`C:\Users\color\Downloads\Trace-20260803T092543.json.gz`

本次只调整结构拖动预览，不改变导图持久化数据、节点提交顺序或叶子节点的原生拖动外观。

## 2. 根因与修复

Trace 包含 20,327 条事件、36 张逐帧截图和 1,087 次 `InputLatency::MouseMove`。调用轨迹包含 `createCloneNode`、`updateOfficialGuideLines` 和候选父节点计算。

根因是 `createCloneNode()` 在上游克隆节点刚创建、但本次 `onMove()` 尚未把克隆放到真实指针坐标时就绘制自定义虚线。首帧读取到克隆初始坐标，虚线因此从画布边缘飞出；下一次移动完成定位后才恢复正确。

修复保持上游顺序：创建克隆时不画目标虚线，第一次有效 `onMove()` 定位并计算候选后再统一绘制。候选父节点直接复用 `simple-mind-map` 的 `highlight()` / `closeHighlight()`，不建立第二套边框；切换目标和拖动结束时恢复原有高亮状态。

含子节点的中间节点会创建子树预览。只给子树根克隆添加 `ymz-drag-subtree-root`，其边框固定为 `#8de2ce`；叶子节点仍走上游原生克隆路径，子节点不继承该主节点边框。

## 3. 新增门禁

- 克隆节点定位前，`updateOfficialGuideLines()` 调用次数必须为零。
- 候选父节点切换时，旧节点恢复既有高亮，新节点只高亮一次；释放后临时类和高亮全部清理。
- 浏览器逐动画帧记录所有显示中的 `stroke-dasharray="6 6"` 路径，路径坐标必须有限且不得触碰居中测试画布边缘。
- 拖动过程中同时存在拖动节点、唯一有效目标虚线和候选父节点高亮。
- 中间节点子树预览只有一个主节点标记，其 `.smm-node-shape` 计算描边必须为 `rgb(141, 226, 206)`。

## 4. 验证结果

| 门禁 | 结果 |
|---|---|
| `npm run check` | 通过 |
| 完整核心测试 | 984 通过、38 个既有跳过、0 失败 |
| 网页单元测试 | 22 通过、0 失败 |
| 完整 Playwright 基线 | 130 通过、86 个策略跳过、0 失败 |
| 拖动定向单元测试 | 52 通过、4 个既有跳过、0 失败 |
| 拖动定向 Chromium E2E | 2 通过、0 失败 |
| 最终 `web-dist` 真实 Chrome 拖动 | 子树 2 节点、主节点 `rgb(141, 226, 206)`、候选父节点 1、虚线 1；释放后临时状态均为 0 |

## 5. 最终构建

- 源码提交：`1347c8e1f3c8cf135fc62f8aaf592db421d48fb1`
- 源码构建标识：`1347c8e1-clean`
- `index.js` SHA256：`838087D0EDFB40C160687C5ED8B42FB4AEEB79345BEE59C4F5878E7124927823`
- 插件 ZIP SHA256：`42EA423F9820800E858F5D4F23CD475717D7208D9DFEB3A05BC90D9FF88F178F`
- 网页 ZIP SHA256：`F8E8EE6D3D41A284A22CD060D118314985DAAE05733EC1EEAB0B05ACD6F30144`
- `npm run release:verify`：通过，两个 ZIP 的文件清单、大小和 SHA256 一致。

以上是标签前本地可复现构建。GitHub 标签工作流从最终构建提交重新生成正式产物，因此正式 ZIP 会写入最终提交标识并具有不同哈希。

## 6. 正式发布与线上一致性

- 最终构建提交：`64aa8d7015c60ae4808717003576cf7e1a1043a4`
- 正式发布构建标识：`64aa8d70-clean`
- GitHub Release：`v1.9.7`
- 正式插件 ZIP SHA256：`9120582A3F2155669ACE7B9E028B1F83A5B11420FB72E08C636569A9F51FC370`
- 正式网页 ZIP SHA256：`20F52338C3175EC5E6B61B2C57533D94A00F798773BFDAD892CF96DFF916F7C8`
- 正式插件 `index.js` SHA256：`7B294CDA983E543E5D104119FBE273AEA39A31F07522619C3289928D54D269CB`
- 正式插件 `index.css` SHA256：`4FA335528B78721A59E2E6A6E997AE3DE7F728AE8F1344A781064BBACD90C344`
- 思源运行目录：版本 `1.9.7`，`index.js` / `index.css` 与正式插件 ZIP 逐文件哈希一致，运行数据目录保留。
- GitHub Pages：`VERSION` 返回 `1.9.7`；真实浏览器显示 `本地网页版 · v1.9.7 · 64aa8d70-clean`，工具栏、导图画布与根节点加载完成，控制台无错误。
