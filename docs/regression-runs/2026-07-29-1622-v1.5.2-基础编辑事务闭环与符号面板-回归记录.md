# YeMind 1.5.2 基础编辑事务闭环与符号面板回归记录

> 基线清单：`docs/standards/回归验收清单.md` v1.1
> 创建时基线提交：`5bc2ef6`
> 候选提交：未提交工作树（按 `YM-DEC-003` 等待用户确认版本与提交）
> 执行档位：发布级自动门禁 + 双端受影响域实机回归

## 变更登记

| 项目 | 内容 |
|---|---|
| 版本/候选版本 | 1.5.2 正式发布候选；由用户于 2026-07-29 明确要求提交发版 |
| 日期 | 2026-07-29 |
| 分支 | main（用户已明确要求继续开发） |
| 修改前提交 SHA | 5bc2ef6 |
| 本次目标 | 修复画布编辑重影/尺寸滞后/清空残层、选择工具栏中间态、ChatGPT 粘贴注释、大纲撤销不同步、反向选区删除失败；重构符号面板；增加构建身份 |
| 共享核心是否变化 | 是 |
| 是否影响数据格式 | 否 |
| 是否影响视觉 | 是 |
| 是否影响交互 | 是 |
| 是否影响插件版 | 是 |
| 是否影响网页版 | 是 |
| 最近同类参考 | 现有 `SymbolPicker` 持久窗口壳层、项目选择面板卡片、Version47 符号分组截图 |
| 复用方式 | 保留现有窗口和插入命令；扩展目录布局模型与卡片 variant |

## 影响域

- [x] 导图画布与节点。
- [x] 大纲。
- [x] 富文本、选区与剪贴板。
- [x] 菜单、弹窗、面板和层级。
- [x] 插件版与网页版适配。
- [x] 数据迁移、持久化和生命周期。
- [x] 构建、运行目录和发布物。

## 定向用例

- [x] `YM-NODE-021` 编辑期间同一 UID 只有一个可见文本层。
- [x] `YM-NODE-022` 输入和粘贴后一帧内节点、编辑器、边框同步。
- [x] `YM-NODE-023` 清空节点后无白色残层且最小尺寸稳定。
- [x] `YM-TEXT-021` 双击全选只发布一次最终工具栏状态。
- [x] `YM-CLIP-021` ChatGPT HTML 注释不进入节点正文。
- [x] `YM-OUTLINE-021` 删除后 Undo/Redo 同时恢复导图和大纲。
- [x] `YM-OUTLINE-022` 反向拖选终点越过方块、彩虹线、拖动柄后 Delete/Backspace 可用。
- [x] `YM-PANEL-021` 符号窗口使用统一内边距、分组卡片和空间布局。
- [x] `YM-BUILD-021` 插件与网页使用同一候选构建，运行目录哈希与候选构建一致。
- [x] `YM-NODE-024` 节点宽度拖动期间外框、文本换行和全树布局实时同步；一次拖动只产生一个可撤销事务。

## 自动化与实机证据

| 门禁 | 结果 | 证据 |
|---|---|---|
| 定向 Vitest | 通过 | 符号布局、空占位、可访问名称定向用例通过 |
| TypeScript/文档 | 通过 | `npm run check`；TypeScript 无错误，文档名称、路径和本地链接有效 |
| 核心测试 | 通过 | `npm test`：15 files，924 passed，45 skipped |
| 网页测试 | 通过 | `npm run test:web`：6 files，22 passed |
| 离线冒烟 | 通过 | `npm run test:offline`：主题、事务、大纲、拖动、资源、交互等全部入口通过 |
| 测试结构 | 通过 | 15 domains，247 scenario modules |
| 资源/版本 | 通过 | 固定本地资源完整；1.5.2 在 8 个版本标记中一致 |
| 双端构建 | 通过 | `npm run build:all`；插件 1151 modules，网页 1140 modules；`node --check index.js` 通过 |
| 浏览器 E2E | 通过 | `npm run test:e2e`：43 passed，7 条按既定移动端适用性跳过，0 failed |
| 网页真实操作 | 通过 | 浏览器 E2E 覆盖几何稳定、粘贴即时扩框、删除、撤销、工具栏、搜索、导入导出面板和符号连续插入 |
| 思源冷启动实机 | 通过 | SiYuan 3.7.3 完全退出后冷启动；旧复杂导图 35 节点正常载入，多行硬换行保留，符号窗口布局与持久关闭规则通过 |
| 运行目录哈希 | 通过 | `index.js`、`index.css`、`plugin.json`、`VERSION` 与候选构建 SHA-256 一致 |

### 2026-07-29 基础编辑事务与剪贴板实机回归

- 画布粘贴：将含末尾换行的 `Alpha\nBeta\n` 粘贴到已有多行节点，节点外框与编辑器立即扩展为两行，无额外空白行；点击空白提交后大纲同步。
- 撤销同步：粘贴后执行 Ctrl+Z，导图与大纲同时恢复原多行内容。
- 大纲快速反向选择：从文字右侧快速拖到左侧彩虹线/拖动区，Delete 仅删除真实文字选区；Ctrl+Z 后导图与大纲同时恢复。
- 硬换行生命周期：折叠、展开、完全退出思源并冷启动后，`Detect ↓ Polling ↓ Configuration ↓ Recovery ↓ L0` 仍保持逐行结构，未出现 `&lt;br&gt;` 或压平成单行。
- 节点宽度：实机完成拉宽、缩窄、提交、连续撤销；拖动过程中外框与文字同步，不再出现节点消失、跳到左上角或旧宽度覆盖实时草稿。
- 符号面板：右键“添加 → 符号”打开统一卡片布局；有左右内边距和空间方向分组；点击画布外不关闭，仅关闭按钮关闭。
- 测试数据：实机修改均已撤销并恢复；测试前备份为 `build/test-backups/maps-20260729-202542.json`。

### 2026-07-29 KMind 对照研究

- 已深度检查 `D:\myDatabase\SiYuan\data\plugins\siyuan-kmind-zen` 的打包运行代码，重点对照硬换行、事务历史、`ResizeObserver`、`requestAnimationFrame` 和编辑器测量闭环。
- 结论与 YeMind 落地规则记录在 `docs/designs/2026-07-29-2203-dev-KMind编辑事务与几何测量对照研究-设计.md`。
- 本轮采用“结构化硬换行 + 单一编辑事务 + 本地实时宽度草稿 + 提交时持久化”的边界，避免继续用字符串替换或拖动期全树重建修补问题。

### 2026-07-29 最终差异审查复核

- 审查曾指出旧富文本自动迁移可能丢失仅由 `class/style/data-*` 表达的格式。最终代码已将带语义属性的元素和未知结构化标签一律视为富文本，并新增仓储级持久化回归。
- `does not discard supported block rich-text formats while migrating existing maps` 定向通过；`<p class="ql-align-center">` 和含 `data-list` 的列表在加载、自动保存后保持原 HTML。
- 审查曾指出画布 Cloze 与大纲 Cloze 规则不同。最终大纲同时识别 `data-yemind-cloze` 与透明文字样式，取消时清除文字色、背景色和空包装元素。
- `recognizes and removes a canvas-created style cloze after switching to the outline` 与完整行边界选择用例定向通过。
- 生成产物漂移问题已通过最终 `npm run build:all`、`node --check index.js`、运行目录同步和四文件 SHA-256 比对消除。

### 2026-07-29 节点宽度专项

- 根因：上游宽度拖动在 `mouseup` 前只更新节点实例的 `customTextWidth`，YeMind 为实时重排触发全树渲染时又从旧 `nodeData.data.customTextWidth` 重建节点，导致拖动过程被旧宽度覆盖。
- 实现：全树布局前仅临时镜像当前实时宽度；在捕获阶段 `mouseup` 恢复拖动前持久值，再由上游命令写入最终宽度，保留单一历史事务。
- 自动化：`v0917UserReportedInteractionRegression.suite.ts` 覆盖实时布局读到新宽度、`mouseup` 前恢复历史输入。
- 网页实机：长节点从约 369px 缩到约 217px、再放宽到约 357px；点击空白后保持，连续 Undo 恢复两次拖动前宽度。
- 思源冷启动实机：在旧导图已有异常小 `customTextWidth` 的节点上完成“拉宽 → 缩窄 → Undo → 再 Undo”，文本换行和相邻节点布局同步更新；最后恢复测试前数据。
- 新构建确认：思源“关于 YeMind”显示源码构建 `5bc2ef6f-dirty-0225f915`，插件声明、运行时代码和构建版本均为 `1.5.1`。
- 运行目录：`D:\myDatabase\SiYuan\data\plugins\siyuan-yemind` 已原位同步；`index.js`、`index.css`、`plugin.json`、`VERSION` 与候选构建哈希一致，`node --check index.js` 通过。

## 当前判定

- [ ] 可以发布
- [x] 有条件发布：代码、双端构建、全量自动门禁、运行目录同步和思源冷启动均已通过；尚未按用户确认创建提交、版本包、GitHub Release 或 Pages 发布。
- [ ] 不可发布

条件说明：

- 版本号已由用户“提交发版”的明确要求授权，从已发布的 `1.5.1` 升级为 `1.5.2`。
- 当前工作树包含本轮累计改动，已经完成最终差异审查和用户发布确认。
- Git 提交、标签、发布包、GitHub Release 与 Pages 的最终地址在发布完成后回填。
