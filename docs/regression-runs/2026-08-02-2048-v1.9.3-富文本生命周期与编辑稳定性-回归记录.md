# YeMind v1.9.3 富文本生命周期与编辑稳定性回归记录

## 1. 基线

- 时间：2026-08-02 20:48:00 +08:00
- 分支：`stabilization/shared-text-layout`
- 修改前提交：`2b093de`
- 候选版本：`v1.9.3`
- 执行档位：Trace 驱动的发布级稳定性修复
- 版本判定：远端 `v1.9.2` 已固定在旧提交 `2b093de`；为避免同名标签对应不同产物，本轮稳定性补丁按 SemVer 发布为 `v1.9.3`，不改写既有标签。

## 2. Trace 证据与根因

输入证据：

- `Trace-20260802T171111.json.gz`
- `Trace-20260802T171727.json.gz`

两份 Trace 均运行 `v1.9.2`、`184345d0-clean` 构建。根因不是旧版逐字符实时 SVG 重建，而是编辑生命周期本身：

1. `TextEdit.show()` 在 Quill 编辑层创建前隐藏静态 SVG 文字，存在两层文字都不可见的同步空档。
2. `RichText.hideEditText()` 在内容未修改时仍执行 `SET_NODE_TEXT`、节点 `group.clear()` 和布局渲染。
3. 初始 `setSelection` 使用 Quill 默认滚动行为，调用 `scrollSelectionIntoView`，会干扰思源宿主视口。
4. 关闭修改内容时，`SET_NODE_TEXT` 已负责必要的节点重绘，随后额外的 `mindMap.render()` 属于重复整图渲染。

## 3. 最小内核差异

- `TextEdit.js`：编辑器完成内容、几何和焦点初始化后才隐藏静态文字。
- `RichText.js`：保存会话初始 HTML；未修改关闭时只恢复静态层，不发命令、不重建节点；修改关闭只保留一次必要提交；焦点使用 `preventScroll` 与静默选区。
- 没有新增 Coordinator、延时、轮询、坐标补偿或第二套渲染生命周期。
- `UPSTREAM_BASELINE.json` 只放行上述两个 Trace 已证明的文件，并由兼容性测试锁定精确清单。

## 4. 后续稳定性收口

在内核原子交接修复后，又按用户真实操作补齐了以下边界：

- 编辑层使用独立、平面的实时节点壳层；输入和粘贴期间由 HTML 编辑器直接承载当前尺寸，已提交 SVG 只作为静态快照，不再出现“文字已变、背后节点框未跟随”。
- Tab、Enter 与节点“+”三条新建路径统一验证：编辑层只要可见，首帧文字就必须已经是“新节点”，不得先出现空白框。
- 选中文字工具栏在节点切换按下时立即隐藏旧实例；选区停止变化 `120ms` 后才按新选区重新测量，并在第一个可见帧完整显示于文字下方。
- 网页存储从预发布 `v1` 到当前 `v2` 采用保留式迁移，旧导图不得因版本升级被清空。
- 长耗时的中心主题几何与跨文件层级剪贴板用例保留原断言，只扩展测试预算；完整四并发回归不得出现重试。

## 5. TDD 与自动化证据

| 门禁 | 结果 | 证据 |
| --- | --- | --- |
| 打开层交接失败用例 | 先红后绿 | 旧实现挂载编辑器时静态层已经不可见；修复后保持可见直到编辑器完成初始化 |
| 未修改关闭失败用例 | 先红后绿 | 旧实现移除节点内部 7 个 SVG 子元素；修复后只移除选择框 `rect`，不移除文字内容组 |
| 上游生命周期 E2E | 10 通过 | 空白帧、焦点、A→B 切换、尺寸提交、删除、Tab/Enter |
| 富文本领域单测 | 61 通过、1 跳过 | 编辑、选区、剪贴板、工具栏、共享排版与上游所有权 |
| 完整 Vitest | 978 通过、38 跳过 | 15 个测试文件、1016 项、0 失败 |
| 核心逐帧 E2E | 9 通过 | 编辑首帧、工具栏首帧与停稳、新节点 Tab/Enter/+ 首帧，0 失败 |
| 完整桌面 Playwright | 105 通过、1 跳过 | 106 项，4 workers，7.4 分钟，0 失败、0 重试 |
| 完整移动 Playwright | 23 通过、83 跳过 | 106 项；跳过项均为明确的桌面专属交互，0 失败 |
| TypeScript 与文档 | 通过 | `npm run check` |
| 插件与网页版构建 | 通过 | `npm run build:all` |

完整 Playwright 覆盖跨文件节点、文字和图片复制粘贴、双击首帧、工具栏首帧、选区删除与 Ctrl+X、宽度拖动、父子拖动、Tab/Enter、重载几何、撤销重做、导入导出等高频操作。

## 6. 最终候选状态

- 最新已测试源码提交：`405dd49`；版本与记录提交：`10f6958`。
- 最终候选 Bundle 标识：`10f69588-clean`。
- 插件根目录与思源运行目录 `index.js` SHA-256：`928e462b93e9941e6386ff616f268c314fb65aca75b8d42581ad7416e8f659c5`，两端完全一致。
- 插件 ZIP：`siyuan-yemind-v1.9.3.zip`，SHA-256 `6ccb16a639d04505ea7d5016a7958c2d834b0497730baebf31e9af652f82c56c`。
- 网页 ZIP：`yemind-web-v1.9.3.zip`，SHA-256 `e8a64c28c2441dcea76a43b025b689474603e83aa8bd74de1f1d67f3c68f3d4e`。
- `release:verify`、固定资源检查和离线烟测全部通过；已同步 809 个白名单文件到思源运行目录。
- 思源运行目录版本为 `1.9.3`。
- GitHub、Release 与 Pages 在最终生成物提交后发布，并以远端工作流结果为最终线上证据。

只有以上项目完成且用户实机不再复现后，才能把正式 `v1.9.3` 标记为发布完成。
