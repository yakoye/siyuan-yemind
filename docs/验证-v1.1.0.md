# YeMind v1.1.0 验证记录

验证日期：2026-07-27

## 自动化验证

- `npm test -- --run`：15 个测试文件通过，658 项通过，45 项按条件跳过。
- `npm run test:web`：5 个测试文件通过，13 项通过。
- `npm run test:offline`：主题、大纲、拖拽、资源、结构与交互冒烟全部通过。
- `npm run check`：TypeScript 类型检查通过。
- `npm run test:syntax`：401 个 TypeScript 文件语法检查通过。
- `npm run test:structure`：15 个测试域、225 个场景模块结构检查通过。
- `npm run check:version`：8 个版本标记均为 `1.1.0`。
- `npm run verify:assets`：固定本地资源完整。
- `npm run build`：思源插件生产构建通过。
- `npm run build:web`：独立网页版生产构建通过；仅保留 Vite 大块体积提示，不影响产物。
- `git diff --check`：通过。

## 真实思源验证

运行目录：

`D:\myDatabase\SiYuan\data\plugins\siyuan-yemind`

验证结果：

- 28 种结构均可在结构面板显示，首组顺序为：右向导图、左向导图、思维导图、逆向导图、双向平衡向下。
- 抽查向上树状图、时间轴、双向组织结构、鱼骨图、圆形图、S 型时间轴、括号图、树形表格，均使用独立几何结构渲染。
- 向上树状图中，根节点与中间节点的 `− / + / 数字` 控件均位于节点上方，不再被横向分布的子节点误导到左侧。
- 大纲内容整体左移，左侧六点拖动柄可见，拖动光标符合移动状态。
- 大纲中的剪贴图可显示八点选中框并直接删除；剪贴图和标记工具栏显示新的替换、删除图标。
- 测试完成后将测试导图恢复为右向导图，并确认保存状态。

## 运行目录同步

- 使用 `npm run sync:runtime:in-place` 同步 810 个运行文件。
- 源目录和运行目录的 `index.js` SHA-256 一致：
  `60F62D2951C93936026C3EBAE4ED6071B46099D117888763A02E4FEBD82C3AB5`
- 运行目录 `VERSION` 与 `plugin.json` 均为 `1.1.0`。
