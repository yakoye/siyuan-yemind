# YeMind 回归清单治理实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 YeMind 累积式回归清单纳入项目版本控制，消除已知产品决策冲突，并建立可追踪、可分级执行的长期验收规则。

**Architecture:** `docs/standards/回归验收清单.md` 是唯一正式基线；仓库外原路径只保留迁移说明。清单使用决策记录、稳定用例编号规范和 P0/影响域/发布级档位管理每次执行范围，具体版本验收仍生成独立证据记录。

**Tech Stack:** Markdown、Git、现有 YeMind 测试与发布命令。

---

### Task 1: 建立唯一正式基线

**Files:**
- Move: `../REGRESSION_CHECKLIST.md`
- Create: `docs/standards/回归验收清单.md`
- Create: `../REGRESSION_CHECKLIST.md`

- [x] **Step 1:** 确认源文件和目标目录的绝对路径均位于 `siyuan-yemind-package` 工作区。
- [x] **Step 2:** 将完整清单迁入 `siyuan-yemind/docs/standards/回归验收清单.md`，不丢失原有条目。
- [x] **Step 3:** 在旧路径创建迁移说明，明确正式文件位置且不保留第二套规则。

### Task 2: 修正规则与增加治理结构

**Files:**
- Modify: `docs/standards/回归验收清单.md`

- [x] **Step 1:** 增加清单版本、唯一正式路径、稳定用例编号和执行档位规则。
- [x] **Step 2:** 增加产品决策记录，登记主题立即应用、富文本工具栏可见层规则和版本确认策略。
- [x] **Step 3:** 将主题验收改为点击主题立即应用并持久化，删除“应用主题”按钮相关规则。
- [x] **Step 4:** 明确富文本工具栏允许透明定位容器，但可见控件必须不透明、可读、无正文穿透。
- [x] **Step 5:** 增加版本号、Git 标签和 Release 需用户确认的发布规则。

### Task 3: 增加本次验收副本规则并验证文档

**Files:**
- Modify: `docs/standards/回归验收清单.md`

- [x] **Step 1:** 规定每次执行建立独立验收记录，永久基线不直接勾选。
- [x] **Step 2:** 检查标题、链接、冲突关键词、重复正式路径和 Markdown 结构。
- [x] **Step 3:** 使用 `git diff --check` 和关键词扫描验证没有旧规则残留。
- [x] **Step 4:** 提交前确认修改只包含治理文档，不修改产品代码。
