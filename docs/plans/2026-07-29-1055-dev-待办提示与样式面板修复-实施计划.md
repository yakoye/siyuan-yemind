# YeMind v1.5.0 待办提示与样式面板修复实施计划

> 依据：`docs/releases/v1.5.0/2026-07-28-1854-v1.5.0-YeMind-v1.5.0界面对齐勾选清单-设计.txt` 第 314—349、461、542、737—769 行，以及用户 2026-07-29 截图反馈。

## 任务 1：锁定根因并补失败测试

1. 在 `tests/suites/outline-split/v0926OutlineContentParity.suite.ts` 增加待办只使用紧凑提示、不进入通用悬浮预览的断言。
2. 在 `tests/suites/styles-themes/v0931ThemePalettePanel.suite.ts` 增加主题卡片选择、应用、选中态和只读态交互测试。
3. 在 `tests/suites/ui-shell/v0925ImportDialogAndDarkPanels.suite.ts` 增加彩虹单行组合控件与四张线型卡断言。
4. 在 `tests/suites/styles-themes/themePresets.suite.ts` 和持久化用例中增加 `polyline` 的归一化与引擎 `lineRadius: 0` 断言。
5. 运行上述目标测试，确认新增断言先失败。

## 任务 2：修复待办与主题交互

1. 修改 `src/editor/outlineAccessories.ts`，为待办提供紧凑 `title`。
2. 修改 `src/editor/YeMindEditor.ts` 的大纲 hover 分流，待办不调用 `NodeHoverPreview`。
3. 修改 `src/ui/projectChoicePanel.ts`，确保主题卡选择和显式应用在 DOM 重绘后仍保持可见选中态与正确回调。
4. 运行目标测试。

## 任务 3：重构彩虹与线型

1. 修改 `src/editor/editorTemplate.ts`，重排彩虹单行控件并增加“折线”卡片。
2. 修改 `src/styles/index.css`，实现 Version47 的单行彩虹控件、四列线型卡和四种预览图形。
3. 扩展 `src/core/themePresets.ts`、`src/model/types.ts`、导入归一化和线型图标，支持 `polyline`。
4. 在 `buildThemeConfig` 中把 `polyline` 转换为引擎 `straight + lineRadius: 0`，把历史 `straight` 保持为圆角折线。
5. 更新右键线型菜单为同样的四项。
6. 运行目标测试、类型检查与构建。

## 任务 4：回归与实机验证

1. 运行 `npm test`、`npm run check`、`npm run build`、网页测试和可执行的 E2E。
2. 同步构建产物到思源运行目录。
3. 在思源中真实点击每个主题、彩虹开关与配色、四种线型，并检查大纲待办提示不遮挡。
4. 将测试结果写入现有 v1.5.0 验证文档；本轮不 commit、不 push。
