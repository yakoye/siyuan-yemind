# YeMind v0.9.20 verification

Verification includes test-structure validation, TypeScript syntax parsing, offline behavior entries, deterministic offline bundle generation, browser smoke tests, ZIP CRC, full extraction, and extracted-package reruns.

Formal Vitest, strict TypeScript and Vite results are reported only when dependencies can be installed; unavailable dependency-backed checks are not represented as passed.
